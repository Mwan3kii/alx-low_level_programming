#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: always 0
 */
int main(void)
{
	char c;

	char f;

	c = 'a';
	f = 'A';

	while
		(c <= 'a') {
			putchar(c);
			c++;
		}
	while
		(f <= 'Z') {
			putchar(f);
			f++;
		}
	putchar('\n');
	return (0);
}
