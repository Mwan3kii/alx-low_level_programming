#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * divi - does division
 * @a: first interger
 * @b: second interger
 *
 * Return: division
 */
int divi(int a, int b)
{
	if (b != 0)
		return (a / b);
	else
		return (0);
}
