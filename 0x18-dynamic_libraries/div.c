#include "main.h"
#include <stdio.h>
/**
 * div - does division
 * @a: first interger
 * @b: second interger
 *
 * Return: division
 */
int div(int a, int b)
{
	if (b != 0)
		return (a / b);
	else
		return (0);
}
